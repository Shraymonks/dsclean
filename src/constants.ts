export enum ApiType {
  Auth = 'SYNO.API.Auth',
  Task = 'SYNO.DownloadStation2.Task',
}

export enum FailedTaskError {
  Success = 0,
}

export enum TaskStatus {
  Seeding = 8,
}

export enum TaskType {
  bt = 'bt',
}

export enum TrackerStatus {
  Success = 'Success',
  Unregistered = 'Unregistered torrent',
}
